import type { Exercise } from "./types";
const countBy=(values:string[])=>Object.fromEntries([...new Set(values)].sort().map((value)=>[value,values.filter((item)=>item===value).length]));
export function getExerciseLibraryStats(exercises:Exercise[]){return{total:exercises.length,byEquipment:countBy(exercises.flatMap((x)=>x.equipment.map((e)=>e.equipmentId))),byCategory:countBy(exercises.map((x)=>x.categoryId)),byDifficulty:countBy(exercises.map((x)=>x.difficulty)),progressions:exercises.flatMap((x)=>x.relationships).filter((x)=>x.type==="progression").length,regressions:exercises.flatMap((x)=>x.relationships).filter((x)=>x.type==="regression").length,substitutions:exercises.flatMap((x)=>x.relationships).filter((x)=>x.type==="substitution").length,variations:exercises.flatMap((x)=>x.relationships).filter((x)=>x.type==="variation").length,aliases:exercises.reduce((sum,x)=>sum+x.aliases.length,0)};}
